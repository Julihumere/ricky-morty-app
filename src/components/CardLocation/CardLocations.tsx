import React from "react";
import { Location } from "../../interfaces/interface";

type Props = {
  locations: Location[];
};

export const CardLocations = ({ locations }: Props) => {
  return <div>CardLocations</div>;
};
